#include <bits/stdc++.h>

#define  rep(i,n1,n2) for(int i=n1;i<n2;i++)

using namespace std;

bool cmp(int a, int b){
	return a>b;
}

int main()
{
	bitset<4> b1(10);
	cout<<b1<<" ";
	std::vector<bitset<16> > vb;
	vb.push_back(10);
	return 0;
}
