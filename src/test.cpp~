#include <bits/stdc++.h>

#define  rep(i,n1,n2) for(int i=n1;i<n2;i++)

using namespace std;

bool cmp(int a, int b){
	return a>b;
}

int main()
{
	/* code */
	std::vector<char> s(1);
	cin>>s[0];
	cout<<sizeof(s);
	return 0;
}
